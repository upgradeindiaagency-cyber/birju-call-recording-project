## Packages
framer-motion | Page transitions and scroll-triggered animations
date-fns | Date formatting

## Notes
- Audio file upload via POST /api/calls/upload expects multipart/form-data.
- Processing might take time; the frontend handles this with a dedicated loading state and spinner.
- Custom fonts: Plus Jakarta Sans (display) and Inter (body) are added to index.css.
